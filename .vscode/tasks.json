{
    "tasks": [
        {
            "type": "shell",
            "label": "Build project",
            "command": "make"
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}